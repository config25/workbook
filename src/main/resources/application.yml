spring:
  datasource:
    url: jdbc:mysql://youngho-db-001.c3g4wc00ay9v.ap-northeast-2.rds.amazonaws.com # jdbc:mysql://localhost:3306/workbook
    username: root
    password: tjljh6767.
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
        hbm2ddl:
          auto: update # create update
        default_batch_fetch_size: 1000

  servlet:
    multipart:
      enabled: true
      maxFileSize: 200MB
      maxRequestSize: 300MB


springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  override-with-generic-response: false


